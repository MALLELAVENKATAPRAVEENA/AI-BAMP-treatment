# AI Models Directory

This folder stores trained machine learning and deep learning model artifacts:
- `ensemble_bamp_model.pkl`: Scikit-Learn & XGBoost trained ensemble model weights.
- `landmark_detector.onnx`: Deep Learning landmark localization network.

Model retraining pipelines and evaluation metrics can be run via `app.py`.
